diff -urpN mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.cpp a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.cpp
--- mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.cpp	2026-01-31 19:42:57.227069687 +0000
+++ a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.cpp	2026-01-31 15:53:14.351004247 +0000
@@ -6,6 +6,7 @@
 #include <sstream>
 #include <memory>
 #include <unistd.h>
+#include <dlfcn.h>
 #include <spdlog/spdlog.h>
 #include <imgui.h>
 #ifdef __linux__
@@ -117,10 +118,96 @@ void imgui_create(void *ctx, const gl_ws
     imgui_init();
     inited = true;
 
-    if (!gladLoadGL())
-        spdlog::error("Failed to initialize OpenGL context, crash incoming");
+    // Load appropriate GL/GLES functions based on platform
+    bool glad_success = false;
+    if (plat == GL_WSI_EGL) {
+        // EGL typically uses OpenGL ES
+        // On PowerVR/GLES, the GL functions are already linked into the application (like libGLESv2.so)
+        // Try GLAD first, but if it fails, manually initialize function pointers
+        SPDLOG_DEBUG("EGL context detected - attempting to load GLES functions");
+        
+        void* egl_loader = get_egl_proc_address_ptr();
+        if (egl_loader) {
+            int result = gladLoadGLES2Loader((GLADloadproc)egl_loader);
+            SPDLOG_DEBUG("gladLoadGLES2Loader result: {}", result);
+            
+            if (result > 0) {
+                glad_success = true;
+            }
+        }
+        
+        // If GLAD failed, manually point GLAD function pointers to the real GL functions
+        // This works because apps link against libGLESv2.so
+        if (!glad_success) {
+            SPDLOG_WARN("GLAD failed, using dlsym fallback for PowerVR");
+            
+            // Static function that GLAD can use as a loader
+            // Uses RTLD_DEFAULT to search all loaded libraries
+            static void* gles_lib_handle = nullptr;
+            
+            // Static function pointer for GLAD
+            static auto gl_loader = [](const char* name) -> void* {
+                static void* gles_handle = nullptr;
+                if (!gles_handle) {
+                    gles_handle = dlopen("libGLESv2.so", RTLD_LAZY | RTLD_GLOBAL);
+                    if (!gles_handle) {
+                        gles_handle = dlopen("libGLESv2.so.2", RTLD_LAZY | RTLD_GLOBAL);
+                    }
+                }
+                
+                // Try RTLD_DEFAULT first (searches all loaded libs)
+                void* func = dlsym(RTLD_DEFAULT, name);
+                if (func) return func;
+                
+                // Try explicit GLES library
+                if (gles_handle) {
+                    func = dlsym(gles_handle, name);
+                    if (func) return func;
+                }
+                
+                // Last resort: eglGetProcAddress
+                void* egl_proc = get_egl_proc_address_ptr();
+                if (egl_proc) {
+                    typedef void* (*eglGetProcAddress_t)(const char*);
+                    func = ((eglGetProcAddress_t)egl_proc)(name);
+                }
+                return func;
+            };
+            
+            // Try to use gladLoadGLES2 with our custom loader
+            int glad_result = gladLoadGLES2Loader(+gl_loader);
+            
+            if (glad_result > 0) {
+                SPDLOG_DEBUG("GLAD loaded with custom loader: {}", glad_result);
+                glad_success = true;
+            } else {
+                SPDLOG_ERROR("GLAD custom loader failed completely");
+            }
+        }
+        
+        if (!glad_success) {
+            SPDLOG_ERROR("Failed to load GLES functions");
+            return;
+        }
+    } else {
+        // GLX uses desktop OpenGL
+        SPDLOG_DEBUG("Loading desktop GL functions for GLX context");
+        glad_success = gladLoadGL();
+    }
+    
+    if (!glad_success) {
+        spdlog::error("Failed to initialize OpenGL context");
+        return;
+    }
 
-    deviceName = (char*)glGetString(GL_RENDERER);
+    const char* renderer_str = (const char*)glGetString(GL_RENDERER);
+    if (!renderer_str) {
+        SPDLOG_ERROR("glGetString(GL_RENDERER) returned NULL");
+        return;
+    }
+    
+    deviceName = renderer_str;
+    
     // If we're running zink we want to rely on the vulkan loader for the hud instead.
     if (deviceName.find("zink") != std::string::npos)
         return;
@@ -129,7 +216,12 @@ void imgui_create(void *ctx, const gl_ws
         sw_stats.version_gl.minor,
         sw_stats.version_gl.is_gles);
 
-    std::string vendor = (char*)glGetString(GL_VENDOR);
+    const char* vendor_str = (const char*)glGetString(GL_VENDOR);
+    if (!vendor_str) {
+        SPDLOG_ERROR("glGetString(GL_VENDOR) returned NULL");
+        return;
+    }
+    std::string vendor = vendor_str;
     SPDLOG_DEBUG("vendor: {}, deviceName: {}", vendor, deviceName);
     sw_stats.deviceName = deviceName;
     if (vendor.find("AMD") != std::string::npos
diff -urpN mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.h a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.h
--- mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.h	2024-06-15 11:27:17.000000000 +0000
+++ a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/gl_hud.h	2026-01-31 04:43:24.554227676 +0000
@@ -23,4 +23,7 @@ void imgui_render(unsigned int width, un
 
 }} // namespace
 
+// Export from inject_egl.cpp for loading GLES
+extern "C" void* get_egl_proc_address_ptr();
+
 #endif //MANGOHUD_GL_IMGUI_HUD_H
diff -urpN mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/inject_egl.cpp a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/inject_egl.cpp
--- mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/inject_egl.cpp	2024-06-15 11:27:17.000000000 +0000
+++ a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/gl/inject_egl.cpp	2026-01-31 15:52:47.034063847 +0000
@@ -16,54 +16,149 @@ using namespace MangoHud::GL;
 
 EXPORT_C_(void *) eglGetProcAddress(const char* procName);
 
-static void* get_egl_proc_address(const char* name) {
+// Export the real eglGetProcAddress for use by GLAD in gl_hud.cpp
+void* get_egl_proc_address_ptr();
 
+static void* get_egl_proc_address(const char* name) {
     void *func = nullptr;
     static void *(*pfn_eglGetProcAddress)(const char*) = nullptr;
-    if (!pfn_eglGetProcAddress) {
+    static int init_attempts = 0;
+    static bool init_done = false;
+    
+    // Initialize eglGetProcAddress with retry logic
+    if (!init_done && init_attempts < 3) {
+        init_attempts++;
+        
+        // Method 1: Try libEGL.so.1 (standard versioned name)
         void *handle = real_dlopen("libEGL.so.1", RTLD_LAZY);
         if (!handle) {
-            SPDLOG_ERROR("Failed to open " MANGOHUD_ARCH " libEGL.so.1: {}", dlerror());
-        } else {
-            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(real_dlsym(handle, "eglGetProcAddress"));
-
-            if(gladLoadGLES2Loader((GLADloadproc)pfn_eglGetProcAddress) == 0) {
-                pfn_eglGetProcAddress = nullptr;
+            // Method 2: Try libEGL.so (unversioned)
+            handle = real_dlopen("libEGL.so", RTLD_LAZY);
+        }
+        
+        if (handle) {
+            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(
+                real_dlsym(handle, "eglGetProcAddress"));
+            
+            if (pfn_eglGetProcAddress) {
+                SPDLOG_DEBUG("Successfully loaded eglGetProcAddress from libEGL");
+                init_done = true;
+            }
+        }
+        
+        // Method 3: Try RTLD_DEFAULT (might already be loaded)
+        if (!pfn_eglGetProcAddress) {
+            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(
+                real_dlsym(RTLD_DEFAULT, "eglGetProcAddress"));
+            
+            if (pfn_eglGetProcAddress) {
+                SPDLOG_DEBUG("Successfully loaded eglGetProcAddress from RTLD_DEFAULT");
+                init_done = true;
+            }
+        }
+        
+        // If still no function, try one more time with RTLD_NEXT
+        if (!pfn_eglGetProcAddress) {
+            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(
+                real_dlsym(RTLD_NEXT, "eglGetProcAddress"));
+            if (pfn_eglGetProcAddress) {
+                SPDLOG_DEBUG("Successfully loaded eglGetProcAddress from RTLD_NEXT");
+            }
+        }
+        
+        // Mark as done after 3 attempts even if failed
+        if (init_attempts >= 3) {
+            init_done = true;
+            if (!pfn_eglGetProcAddress) {
+                SPDLOG_WARN("Could not load eglGetProcAddress after {} attempts", init_attempts);
             }
         }
     }
-
-    if (pfn_eglGetProcAddress)
+    
+    // Use eglGetProcAddress if successfully loaded
+    if (pfn_eglGetProcAddress) {
         func = pfn_eglGetProcAddress(name);
-
-    if (!func)
-        func = get_proc_address( name );
-
+        if (func) {
+            SPDLOG_TRACE("get_egl_proc_address('{}') via eglGetProcAddress = {}", name, func);
+            return func;
+        }
+    }
+    
+    // Fallback to standard dlsym approach
+    func = get_proc_address(name);
+    
     if (!func) {
-        SPDLOG_ERROR("Failed to get function '{}'", name);
+        SPDLOG_DEBUG("Failed to get EGL function '{}'", name);
+    } else {
+        SPDLOG_TRACE("get_egl_proc_address('{}') via get_proc_address = {}", name, func);
     }
-
+    
     return func;
 }
 
+// Export the real eglGetProcAddress function pointer for GLAD to use
+void* get_egl_proc_address_ptr() {
+    // Trigger initialization if needed
+    get_egl_proc_address("eglGetCurrentContext");
+    
+    // Return the actual function pointer after ensuring it's loaded
+    static void *(*pfn_eglGetProcAddress)(const char*) = nullptr;
+    if (!pfn_eglGetProcAddress) {
+        void *handle = real_dlopen("libEGL.so.1", RTLD_LAZY);
+        if (!handle) handle = real_dlopen("libEGL.so", RTLD_LAZY);
+        if (handle) {
+            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(
+                real_dlsym(handle, "eglGetProcAddress"));
+        }
+        if (!pfn_eglGetProcAddress) {
+            pfn_eglGetProcAddress = reinterpret_cast<decltype(pfn_eglGetProcAddress)>(
+                real_dlsym(RTLD_DEFAULT, "eglGetProcAddress"));
+        }
+    }
+    return (void*)pfn_eglGetProcAddress;
+}
+
 EXPORT_C_(unsigned int) eglSwapBuffers( void* dpy, void* surf);
 EXPORT_C_(unsigned int) eglSwapBuffers( void* dpy, void* surf)
 {
+    // eglSwapBuffers is a core EGL function - load it from libEGL.so via dlsym
     static int (*pfn_eglSwapBuffers)(void*, void*) = nullptr;
-    if (!pfn_eglSwapBuffers)
-        pfn_eglSwapBuffers = reinterpret_cast<decltype(pfn_eglSwapBuffers)>(get_egl_proc_address("eglSwapBuffers"));
+    if (!pfn_eglSwapBuffers) {
+        void* egl_handle = real_dlopen("libEGL.so.1", RTLD_LAZY);
+        if (!egl_handle) egl_handle = real_dlopen("libEGL.so", RTLD_LAZY);
+        if (egl_handle) {
+            pfn_eglSwapBuffers = reinterpret_cast<decltype(pfn_eglSwapBuffers)>(real_dlsym(egl_handle, "eglSwapBuffers"));
+        }
+        if (!pfn_eglSwapBuffers) {
+            // Fallback to RTLD_DEFAULT
+            pfn_eglSwapBuffers = reinterpret_cast<decltype(pfn_eglSwapBuffers)>(real_dlsym(RTLD_DEFAULT, "eglSwapBuffers"));
+        }
+    }
 
     if (!is_blacklisted()) {
+        // eglQuerySurface is a core EGL function, not an extension
+        // It should be loaded from libEGL.so via dlsym, not eglGetProcAddress
         static int (*pfn_eglQuerySurface)(void* dpy, void* surface, int attribute, int *value) = nullptr;
-        if (!pfn_eglQuerySurface)
-            pfn_eglQuerySurface = reinterpret_cast<decltype(pfn_eglQuerySurface)>(get_egl_proc_address("eglQuerySurface"));
+        if (!pfn_eglQuerySurface) {
+            void* egl_handle = real_dlopen("libEGL.so.1", RTLD_LAZY);
+            if (!egl_handle) egl_handle = real_dlopen("libEGL.so", RTLD_LAZY);
+            if (egl_handle) {
+                pfn_eglQuerySurface = reinterpret_cast<decltype(pfn_eglQuerySurface)>(real_dlsym(egl_handle, "eglQuerySurface"));
+            }
+            if (!pfn_eglQuerySurface) {
+                // Fallback to RTLD_DEFAULT
+                pfn_eglQuerySurface = reinterpret_cast<decltype(pfn_eglQuerySurface)>(real_dlsym(RTLD_DEFAULT, "eglQuerySurface"));
+            }
+        }
 
         imgui_create(surf, gl_wsi::GL_WSI_EGL);
 
         int width=0, height=0;
-        if (pfn_eglQuerySurface(dpy, surf, 0x3056, &height) &&
-            pfn_eglQuerySurface(dpy, surf, 0x3057, &width))
+        if (pfn_eglQuerySurface && 
+            pfn_eglQuerySurface(dpy, surf, 0x3056, &height) &&
+            pfn_eglQuerySurface(dpy, surf, 0x3057, &width)) {
             imgui_render(width, height);
+        }
 
         using namespace std::chrono_literals;
         if (fps_limit_stats.targetFrameTime > 0s && fps_limit_stats.method == FPS_LIMIT_METHOD_EARLY){
@@ -117,8 +212,14 @@ EXPORT_C_(void *) eglGetProcAddress(cons
     void* real_func = get_egl_proc_address(procName);
     void* func = mangohud_find_egl_ptr(procName);
     SPDLOG_TRACE("{}: proc: {}, real: {}, fun: {}", __func__, procName, real_func, func);
-    if (func && real_func)
+    if (func && real_func) {
+        SPDLOG_DEBUG("eglGetProcAddress('{}') returning MangoHud hook", procName);
         return func;
+    }
+    
+    if (real_func) {
+        SPDLOG_DEBUG("eglGetProcAddress('{}') returning real function", procName);
+    }
 
     return real_func;
 }
diff -urpN mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/hook_dlsym.cpp a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/hook_dlsym.cpp
--- mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/hook_dlsym.cpp	2024-06-15 11:27:17.000000000 +0000
+++ a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/src/hook_dlsym.cpp	2026-01-31 15:22:19.302376925 +0000
@@ -1,39 +1,106 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <dlfcn.h>
+#include <cstring>
 #include "real_dlsym.h"
 
+// Helper to detect EGL functions
+static inline bool is_egl_function_name(const char* name) {
+    if (!name) return false;
+    return (name[0] == 'e' || name[0] == 'E') && 
+           (strncmp(name, "egl", 3) == 0 || 
+            strncmp(name, "Egl", 3) == 0 || 
+            strncmp(name, "EGL", 3) == 0);
+}
+
 EXPORT_C_(void*) dlsym(void * handle, const char * name)
 {
     static void *(*find_glx_ptr)(const char *name) = nullptr;
     static void *(*find_egl_ptr)(const char *name) = nullptr;
-
-    if (!find_glx_ptr)
-        find_glx_ptr = reinterpret_cast<decltype(find_glx_ptr)> (real_dlsym(RTLD_NEXT, "mangohud_find_glx_ptr"));
-
-    if (!find_egl_ptr)
-        find_egl_ptr = reinterpret_cast<decltype(find_egl_ptr)> (real_dlsym(RTLD_NEXT, "mangohud_find_egl_ptr"));
+    static int lookup_attempts = 0;
+    static bool init_printed = false;
+    static bool initialized = false;
+
+    // Retry lookups a few times in case the main library loads later
+    // (e.g., when only dlsym wrapper is preloaded initially)
+    if (!initialized && lookup_attempts < 10) {
+        if (!init_printed) {
+            fprintf(stderr,"[MangoHud] dlsym hook: Initializing, searching for MangoHud exports...\n");
+            init_printed = true;
+        }
+        
+        if (!find_glx_ptr) {
+            // Try RTLD_NEXT first, then RTLD_DEFAULT for all loaded libraries
+            find_glx_ptr = reinterpret_cast<decltype(find_glx_ptr)> (real_dlsym(RTLD_NEXT, "mangohud_find_glx_ptr"));
+            if (!find_glx_ptr) {
+                find_glx_ptr = reinterpret_cast<decltype(find_glx_ptr)> (real_dlsym(RTLD_DEFAULT, "mangohud_find_glx_ptr"));
+            }
+            if (find_glx_ptr) {
+                fprintf(stderr,"[MangoHud] dlsym hook: Found mangohud_find_glx_ptr = %p at attempt %d\n", (void*)find_glx_ptr, lookup_attempts);
+            }
+        }
+        if (!find_egl_ptr) {
+            // Try RTLD_NEXT first, then RTLD_DEFAULT for all loaded libraries
+            find_egl_ptr = reinterpret_cast<decltype(find_egl_ptr)> (real_dlsym(RTLD_NEXT, "mangohud_find_egl_ptr"));
+            if (!find_egl_ptr) {
+                find_egl_ptr = reinterpret_cast<decltype(find_egl_ptr)> (real_dlsym(RTLD_DEFAULT, "mangohud_find_egl_ptr"));
+            }
+            if (find_egl_ptr) {
+                fprintf(stderr,"[MangoHud] dlsym hook: Found mangohud_find_egl_ptr = %p at attempt %d\n", (void*)find_egl_ptr, lookup_attempts);
+            }
+        }
+        
+        // Consider initialized if we found at least one (GLX or EGL)
+        // Many systems are EGL-only and will never have GLX
+        if (find_glx_ptr || find_egl_ptr) {
+            initialized = true;
+            fprintf(stderr,"[MangoHud] dlsym hook: Initialization complete (GLX=%s, EGL=%s)\n",
+                    find_glx_ptr ? "YES" : "NO",
+                    find_egl_ptr ? "YES" : "NO");
+        } else {
+            lookup_attempts++;
+            if (lookup_attempts >= 10) {
+                fprintf(stderr,"[MangoHud] dlsym hook: WARNING - Failed to find any MangoHud exports after %d attempts!\n", lookup_attempts);
+                initialized = true; // Stop trying
+            }
+        }
+    }
 
     void* func = nullptr;
+    void* real_func = nullptr;
+    
+    // Check for name validity first
+    if (!name) {
+        return real_dlsym(handle, name);
+    }
+    
     bool is_angle = real_dlsym(handle, "eglStreamPostD3DTextureANGLE");
-    void* real_func = real_dlsym(handle, name);
+    real_func = real_dlsym(handle, name);
 
+    // Try GLX hooks first for GLX functions (only if initialized)
     if (find_glx_ptr && real_func) {
         func = find_glx_ptr(name);
         if (func) {
-            //fprintf(stderr,"%s: local: %s\n",  __func__ , name);
+            //fprintf(stderr,"[MangoHud] dlsym hook: returning GLX '%s'\n", name);
             return func;
         }
     }
 
+    // For EGL functions, prioritize eglGetProcAddress-based lookup (only if initialized)
     if (find_egl_ptr && real_func && !is_angle) {
         func = find_egl_ptr(name);
         if (func) {
-            //fprintf(stderr,"%s: local: %s\n",  __func__ , name);
+            //fprintf(stderr,"[MangoHud] dlsym hook: returning EGL '%s'\n", name);
             return func;
         }
     }
 
+    // If we're looking for an EGL function and didn't find a hook,
+    // but also didn't find it via dlsym, this might be a PowerVR issue
+    if (is_egl_function_name(name) && !real_func && find_egl_ptr) {
+        fprintf(stderr,"[MangoHud] dlsym hook: EGL function '%s' returned NULL from dlsym (PowerVR?)\n", name);
+    }
+
     //fprintf(stderr,"%s: foreign: %s\n",  __func__ , name);
     return real_func;
 }
diff -urpN mangohud-12620c91eaca0917a7939a92ec33915cadf24475/bin/mangohud.in a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/bin/mangohud.in
--- mangohud-12620c91eaca0917a7939a92ec33915cadf24475/bin/mangohud.in	2024-06-15 11:27:17.000000000 +0000
+++ a133_mangohud-12620c91eaca0917a7939a92ec33915cadf24475/bin/mangohud.in	2026-01-31 19:57:15.430874317 +0000
@@ -22,6 +22,9 @@ elif [ "$MANGOHUD_DLSYM" = "1" ]; then
     MANGOHUD_LIB_NAME="@ld_libdir_mangohud@libMangoHud_dlsym.so:${MANGOHUD_LIB_NAME}"
 fi
 
+# A133, we need both libraries regardless of the mode
+MANGOHUD_LIB_NAME="@ld_libdir_mangohud@libMangoHud_dlsym.so:${MANGOHUD_LIB_NAME}"
+
 if [ "$1" = "--version" ]; then
     echo @version@
     exit 0
